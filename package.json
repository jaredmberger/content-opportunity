{
  "name": "content-opportunity",
  "version": "0.1.0",
  "private": true,
  "description": "CuratorOS Content Opportunity Finder — explainable editorial opportunity discovery for oceanliners.net.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check worker.js && node --check src/scoring.js && node --check src/discovery.js"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
